# 执行模块提交清单（当前为仿真）

对应答疑 Q9。真机未交；执行 = 仿真复现。

| Q9 | 本模块交什么 |
|----|----------------|
| ① 推理 | `sim/run_agent_pick_place_side_sim.py`、`sim/ec/run_ec_bin_sim.py`；读任务 JSON 并跑抓放。无执行用 `.pth` |
| ① 独立运行 | 上面两条命令，或由 `decision/agent_demo_ui.py` 拉起 |
| ② 训练 | 无网络训练。`sim/grasp/*.pickle` 为抓取库 |
| ③ 仿真环境 | 本目录 + `dataset_learn` + `Part_Model`；Python + Panda3D + WRS |
| ④ 视频 | 界面下指令后仿真运动 |
| ⑤ 报告 | 总报告中的执行/仿真章节 |
| ⑥ 说明 | `sim/使用说明.md` |

不要把 `dataset_learn` 原图整包塞进本文件夹。
