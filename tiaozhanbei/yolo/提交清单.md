# 感知模块提交清单

对应答疑 Q9。主文档：`使用说明.md`。

| Q9 | 本模块交什么 |
|----|----------------|
| ① 推理 | `仿真/infer_seg_annotations.py` + `仿真/best.pt`（可选 `权重/yolo26s-seg-tiny19.pt`）；实机另用 `实机/` |
| ① 独立运行 | `--sample 0001` 写出 `仿真/outputs/0001.json` |
| ② 训练 | 仿真：`train_annotations_yolo.py`、`train_state_head.py`（数据 `dataset_learn`）；实机：`train_real_merged_yolo.py`（数据 `实机/dataset/`） |
| ③ 仿真 | 场景数据在 `dataset_learn/`，不在本目录重复采集 |
| ⑤ 报告 | 总报告第 2 章 |
| ⑥ 说明 | `使用说明.md`、`实机/使用说明.md` |

不要把 `dataset_learn` 原图整包塞进本文件夹。仿真 `best.pt` 与实机 `best.pt` 不要混用。
